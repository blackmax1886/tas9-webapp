mutation createTask($task: NewTask!) {
  createTask(input: $task) {
    id
    name
    done
    archived
  }
}

query getTasks($userId: String!) {
  tasks(userId: $userId) {
    id
    name
    content
  }
}
